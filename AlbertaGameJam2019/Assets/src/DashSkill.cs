using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.AI;

public class DashSkill //: PlayerSkill
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    //public override Timer Cooldown { get; set; }
    //private Timer _time = new Timer(5f);

    //protected override void Behave(GameObject User)
    //{
      //  NavMeshAgent agent = GetComponent<NavMeshAgent>();
        //agent.velocity = new Vector3(Input.GetAxis("Horizontal"), 0.0f, Input.GetAxis("Vertical"));
        //moveDirection *= speed * 10;
        //agent.velocity = moveDirection;

    //}

    void Update()
    {
        
    }
}
